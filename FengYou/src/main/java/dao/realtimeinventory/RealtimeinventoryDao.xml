<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.realtimeinventory.RealtimeinventoryDao">
	
	
	<select id="queryRealtimeinventoryByHHid" resultType="Realtimeinventory" parameterType="Realtimeinventory">
		SELECT * FROM Realtimeinventory where 1=1
		<if test="hotelId!=0">
		and hotelId =#{hotelId}
		</if>
		<if test="houseId!=0">
		and houseId =#{houseId}
		</if>
		
	</select>
</mapper>